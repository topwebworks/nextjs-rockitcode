# cPanel Email Configuration for RockitCode
# Add these to your .env.local file

# =================================
# cPanel SMTP Configuration
# =================================

# Method 1: SSL (Port 465) - Most Secure
SMTP_HOST=mail.rockitcode.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=impact@rockitcode.com
SMTP_PASS=your_email_password

# Method 2: TLS (Port 587) - Alternative
# SMTP_HOST=mail.rockitcode.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=impact@rockitcode.com
# SMTP_PASS=your_email_password

# Method 3: Non-SSL (Port 25/2525) - Least Secure
# SMTP_HOST=mail.rockitcode.com
# SMTP_PORT=25
# SMTP_SECURE=false
# SMTP_USER=impact@rockitcode.com
# SMTP_PASS=your_email_password

# =================================
# How to Find Your cPanel Settings
# =================================

# 1. Login to cPanel
# 2. Go to "Email Accounts" 
# 3. Click "Configure Mail Client" next to your email
# 4. Use the "Mail Client Manual Settings"

# Common cPanel SMTP hosts:
# - mail.yourdomain.com
# - server.yourhostingprovider.com
# - Your server's IP address

# =================================
# Testing Configuration
# =================================

# Test with a simple script:
# node -e "
# const nodemailer = require('nodemailer');
# const transporter = nodemailer.createTransporter({
#   host: 'mail.rockitcode.com',
#   port: 465,
#   secure: true,
#   auth: { user: 'impact@rockitcode.com', pass: 'your_password' }
# });
# transporter.sendMail({
#   from: 'impact@rockitcode.com',
#   to: 'test@example.com',
#   subject: 'Test',
#   text: 'Hello World'
# }).then(console.log).catch(console.error);
# "
