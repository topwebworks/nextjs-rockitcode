# Alternative cPanel Email Configuration
# Use this if you ever need to switch from Gmail OAuth2 to cPanel email

# cPanel SMTP Settings (replace with your actual values)
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=noreply@yourdomain.com
SMTP_PASS=your_cpanel_email_password

# Alternative port configurations:
# Port 587 (TLS) - Recommended
# Port 465 (SSL) - Use SMTP_SECURE=true
# Port 25 (Non-encrypted) - Not recommended

# Note: Currently using Gmail OAuth2 configuration in .env.local
# This file is for reference/backup purposes only